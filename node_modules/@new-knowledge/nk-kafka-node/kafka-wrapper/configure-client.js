let clientConfig = require('./client-config');

function configureClient(kafkaClientConfig) {
  clientConfig = Object.assign({}, clientConfig, kafkaClientConfig);
}

module.exports = configureClient;
