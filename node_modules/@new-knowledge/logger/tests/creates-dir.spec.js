const fs = require('fs');
const path = require('path');

require('../index.js');

test('Logs directory should be defined', () => {
  const fileStat = fs.statSync(path.resolve('./logs'));
  expect(fileStat.isDirectory()).toEqual(true);
});
